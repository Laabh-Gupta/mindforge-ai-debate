services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file: backend/.env
    ports:
      - "4000:4000"
    restart: unless-stopped
# All persistent data lives in the configured Supabase PostgreSQL database.
